# 版本升级

大多数版本升级只需在拉取最新版本后重新部署核心容器，并且与外围客户端完全向后兼容，外围客户端可以方便地稍后更新。这是默认情况，除非在[版本发布说明](https://github.com/moghtech/komodo/releases)中明确提及。

某些核心 API 升级可能会改变构建/克隆等行为，并且需要更新外围二进制文件以匹配核心版本，然后才能恢复此功能。这将在发布说明中明确提及。